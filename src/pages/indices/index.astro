---
import Base from "../../layouts/Base.astro";
import { citations, citationsOf, dataset, type RankingCategory } from "../../lib/rankings";
import { indexHref } from "../../lib/paths";

const groups: { key: RankingCategory; label: string }[] = [
  { key: "overall", label: "Overall world rankings" },
  { key: "subject", label: "Subject rankings" },
  { key: "sdg", label: "Sustainable Development Goal rankings" },
  { key: "thematic", label: "Thematic rankings" },
];

const rows = dataset.rankings.map((r) => ({
  ranking: r,
  citations: citationsOf(r.id).length,
}));

const citedCount = rows.filter((r) => r.citations > 0).length;
const attributed = citations.filter((c) => c.ranking !== undefined).length;

const sections = groups
  .map((g) => ({
    ...g,
    rows: rows
      .filter((row) => (row.ranking.category ?? "overall") === g.key)
      .toSorted((a, b) => b.citations - a.citations),
  }))
  .filter((g) => g.rows.length > 0);
---

<Base
  title="The indices — The World University Index"
  description={`The ${dataset.rankings.length} ranking tables the Index draws on, and which institutions have been recorded citing each one.`}
>
  <article class="indices">
    <div class="container">
      <p class="eyebrow">The evidence base</p>
      <h1>The indices</h1>

      <p class="lead">
        The {dataset.rankings.length} ranking tables the Index draws on. Alongside
        each, the institutions recorded citing it — the backlink that turns a
        table into an index somebody cares about.
      </p>

      <dl class="tally">
        <div class="tally__item">
          <dt>Tables recorded</dt>
          <dd>{dataset.rankings.length}</dd>
        </div>
        <div class="tally__item">
          <dt>Cited by an institution</dt>
          <dd>{citedCount}</dd>
        </div>
        <div class="tally__item">
          <dt>Claims on the record</dt>
          <dd>{citations.length}</dd>
        </div>
      </dl>

      <p class="note">
        A table nobody quotes is a spreadsheet. A table one university has put on
        its own website is an index that matters to at least somebody, and the
        Index treats it accordingly — the obscure ones most of all, since an
        institution that cites a ranking almost nobody has heard of has told you
        something a well-known ranking never could. {attributed} of the {
          citations.length
        } claims on record name a specific table; the rest cite a publisher's
        rankings in general and are shown on the institution's own page.
      </p>

      {
        sections.map((g) => (
          <section class="group">
            <h2 class="group__heading">
              {g.label} <span class="group__count">{g.rows.length}</span>
            </h2>
            <ul class="list">
              {g.rows.map(({ ranking, citations: n }) => (
                <li class="row">
                  <a class="row__link" href={indexHref(ranking.id)}>
                    {ranking.scope ?? ranking.name}
                  </a>
                  <span class="row__meta">
                    {ranking.shortName} {ranking.edition}
                  </span>
                  {n > 0 ? (
                    <span class="row__cites">
                      cited &times;{n}
                    </span>
                  ) : (
                    <span class="row__uncited">uncited</span>
                  )}
                </li>
              ))}
            </ul>
          </section>
        ))
      }
    </div>
  </article>
</Base>

<style>
  .indices {
    padding-block: clamp(1.5rem, 1rem + 2vw, 3rem);
    max-width: 52rem;
  }

  .eyebrow {
    margin: 0;
    font-family: var(--sans);
    font-size: 0.78rem;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    color: var(--ink-faint);
  }

  h1 {
    margin: 0.3rem 0 0;
    font-size: clamp(2.2rem, 1.4rem + 3vw, 3rem);
  }

  .lead {
    font-size: 1.15rem;
    color: var(--ink-soft);
    margin: 1rem 0 0;
    max-width: 44rem;
  }

  .tally {
    display: flex;
    flex-wrap: wrap;
    gap: 1.75rem;
    margin: 1.5rem 0 0;
    padding: 1rem 1.25rem;
    background: var(--paper);
    border: 1px solid var(--rule);
    border-radius: var(--radius);
  }

  .tally__item {
    margin: 0;
  }

  .tally dt {
    font-family: var(--sans);
    font-size: 0.75rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--ink-faint);
  }

  .tally dd {
    margin: 0.15rem 0 0;
    font-size: 1.9rem;
    font-weight: 600;
    font-variant-numeric: tabular-nums;
    color: var(--accent);
    line-height: 1;
  }

  .note {
    margin: 1.1rem 0 0;
    max-width: 44rem;
    font-size: 0.95rem;
    color: var(--ink-soft);
  }

  .group {
    margin-top: 2.25rem;
  }

  .group__heading {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    margin: 0 0 0.4rem;
    font-size: 1.3rem;
    padding-bottom: 0.35rem;
    border-bottom: 1px solid var(--rule);
  }

  .group__count {
    font-family: var(--sans);
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--ink-faint);
    font-variant-numeric: tabular-nums;
  }

  .list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .row {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 0.3rem 0.7rem;
    padding: 0.45rem 0;
    border-bottom: 1px solid var(--rule);
  }

  .row__link {
    font-size: 1rem;
  }

  .row__meta {
    font-family: var(--sans);
    font-size: 0.8rem;
    color: var(--ink-faint);
    margin-right: auto;
  }

  .row__cites,
  .row__uncited {
    font-family: var(--sans);
    font-size: 0.72rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .row__cites {
    color: var(--accent);
    border: 1px solid currentcolor;
    border-radius: var(--radius);
    padding: 0.05rem 0.35rem;
  }

  .row__uncited {
    color: var(--ink-faint);
  }
</style>
